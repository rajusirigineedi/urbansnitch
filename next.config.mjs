/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.nicobar.com','assets.ajio.com','encrypted-tbn0.gstatic.com'],
      },
};

export default nextConfig;
